<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="check.CheckDAO">
<select id="checklist" resultType="checkdto">
	select checktitle, checkdesc, checkimg from checks
</select>
<insert id="insertCheck" parameterType="checkdto">
	insert into checks(nickname, chnum, checknum, checktime, checktitle, checkdesc, checkimg)
	values(#{nickname}, #{chnum}, #{checknum}, #{checktime}, #{checktitle}, #{checkdesc}, #{checkimg.originalFilename})  
</insert>
	

</mapper>