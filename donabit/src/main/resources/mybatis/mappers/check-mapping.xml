<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="check.CheckDAO">
	<select id="checklist" resultType="checkdto">
		SELECT checktitle, checkdesc, checkimg AS checkimg2, checktime FROM checks ORDER BY checktime DESC
	</select>
	
	<insert id="insertCheck" parameterType="checkdto">
		INSERT INTO checks(nickname, chnum, checknum, checktime, checktitle, checkdesc, checkimg)
		VALUES(#{nickname}, #{chnum}, #{checknum}, now(), #{checktitle}, #{checkdesc}, #{checkimg2})  
	</insert>
	
	<!-- <select id="selectcountchallenge" resultType="int" parameterType="checkdto">
		SELECT COUNT(*) FROM checks WHERE nickname=#{nickname} AND chnum=#{chnum};
	</select> -->
	
	<select id="selectcountchallenge" resultType="int">
		SELECT COUNT(*) FROM checks WHERE nickname=1 AND chnum=2;
	</select>
	
	<select id="selectchsuccess" resultType="int">
		SELECT chsuccess FROM challenge WHERE chnum=2;
	</select>
	
	<update id="updatepersonalpf" parameterType="int">
		UPDATE challenging SET personalpf=1 WHERE chnum=2;
	</update>
	
	<select id="selectchecktime" resultType="int">
		SELECT COUNT(*) FROM checks WHERE chnum=2 AND DATE(checktime)=DATE(now());
	</select>
</mapper>